{
  "name": "lyon-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend Cloudflare Worker pour la carte temps réel TCL — agrège les données ouvertes SYTRAL / Grand Lyon (remplace lyon-server).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
